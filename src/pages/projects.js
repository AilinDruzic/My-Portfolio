import HorizontalScrollCarousel from "@/components/HorizontalScrollCarousel";

export default function Projects() {
  return (
    <section
      className="place-items-center text-3xl font-bold"
      id="Projects"
    > 
      <HorizontalScrollCarousel />
    </section>
  );
}
